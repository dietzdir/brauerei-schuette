rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /products/{imageId} {
      // Anyone can see product images
      allow read: if true;
      // Only admins can upload/delete product images
      allow write: if request.auth != null && request.auth.token.admin == true;
    }

    match /rentals/{imageId} {
      // Anyone can see rental / equipment images
      allow read: if true;
      // Only admins can upload/delete rental images
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}

